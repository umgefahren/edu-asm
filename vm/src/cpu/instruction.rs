use crate::{state::VmState, register_spec::WriteableReadable};

pub fn add_t_s(state: &mut VmState, parameters: &[Box<dyn WriteableReadable>]) {
}

pub fn add_i_s(state: &mut VmState, parameters: &[Box<dyn WriteableReadable>]) {
}

pub fn add_t_u(state: &mut VmState, parameters: &[Box<dyn WriteableReadable>]) {
}
